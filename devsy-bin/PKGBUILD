# Maintainer: Shotor (AUR: Shotor) <dev [at] shotor [dot] com>

pkgname=devsy-bin
pkgver=1.16.1
pkgrel=1
pkgdesc="CLI for Devsy"
arch=('x86_64')
url="https://devsy.sh"
license=('MPL-2.0')

conflicts=('devsy')
provides=('devsy')

options=('!strip' '!debug')

source=(
  "devsy::https://github.com/devsy-org/devsy/releases/download/v${pkgver}/devsy-linux-amd64"
)

sha256sums=(
  '79eeb50d25363d6eb682dca53be78ef717b001f0d8d1f636ba3179e204e162fe'
)

package() {
  install -Dm755 \
    "$srcdir/devsy" \
    "$pkgdir/usr/bin/devsy"
}
